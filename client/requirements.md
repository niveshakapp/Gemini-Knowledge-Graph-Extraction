## Packages
framer-motion | Page transitions and complex animations
recharts | Dashboard charts for extraction statistics
date-fns | Formatting timestamps in tables and logs
clsx | Utility for conditional class names (usually installed, but good to ensure)
tailwind-merge | Utility for merging tailwind classes (usually installed)
lucide-react | Icons (base stack, but essential)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  mono: ["JetBrains Mono", "monospace"],
  display: ["Space Grotesk", "sans-serif"],
}
